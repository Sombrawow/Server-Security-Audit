# Remediation Checklist — shadowserver

Use this file to track remediation progress. Update status as each item is completed.

## Phase 1 — Critical (apply immediately)

- [ ] **CRIT-01** — Remove `SaveConfig = true` from `/etc/wireguard/wg0.conf`
- [ ] **CRIT-02** — Add `ListenAddress 10.8.0.1` and `ListenAddress 192.168.0.16` to `/etc/ssh/sshd_config`
- [ ] **CRIT-02** — Remove `22/tcp ALLOW IN Anywhere` and `2222/tcp ALLOW IN Anywhere` from UFW
- [ ] **MED-05** — Set `X11Forwarding no` in `/etc/ssh/sshd_config`
- [ ] **LOW-03** — `sudo ufw logging medium`
- [ ] **LOW-01** — `sudo systemctl disable --now cups cups-browsed`
- [ ] **MED-02** — `sudo systemctl disable --now avahi-daemon`
- [ ] **LOW-02** — `sudo chmod u-s /usr/bin/pkexec`

## Phase 2 — High (next maintenance window / weekend)

- [ ] **HIGH-02** — Determine if Apache/Nextcloud is still needed; disable if not
- [ ] **HIGH-03** — Determine if MariaDB is still needed; disable if not
- [ ] **HIGH-04** — `sudo systemctl disable --now postfix`
- [ ] **HIGH-01** — Fix UFW rule for RDP: restrict to `wg0` only
- [ ] **MED-04** — Full UFW ruleset reset and rebuild per report recommendations

## Phase 3 — Medium/Low (next weekend)

- [ ] **MED-01** — Add `interfaces = lo wg0 eno1` and `bind interfaces only = yes` to `/etc/samba/smb.conf`
- [ ] **MED-01** — `sudo systemctl disable --now winbind`
- [ ] **MED-03** — Clean up duplicate crontab entries (root and user)
- [ ] **MED-06** — Document or remove WireGuard peer `10.8.0.3` (84.125.103.70)
- [ ] **LOW-04** — Disable `check-nextcloud.timer` if Nextcloud is not running
- [ ] **LOW-04** — Remove `nextcloud-backup.sh` cron entry if Nextcloud is absent

## Notes

- Always test SSH connectivity from a second terminal before closing current session after sshd changes
- Apply UFW changes during market close (weekends) to avoid disrupting trading connectivity
- After rotating WireGuard key (CRIT-01): update all peer devices with new server public key